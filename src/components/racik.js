import ReactTypingEffect from "react-typing-effect";
import axios from "axios";
import { useState } from "react";
import { Container, Form, Button } from "react-bootstrap";

function Racik() {
  // const inputUrea = (Kilogram) => {
  //   const berat = Kilogram;
  //   return berat;
  // };
  // const inputSP = (Kilogram) => {
  //   const berat = Kilogram;
  //   return berat;
  // };
  // const inputKCL = (Kilogram) => {
  //   const berat = Kilogram;
  //   return berat;
  // };

  // const hasil = (inputUrea, inputSP, inputKCL) => {
  //   const Urea = 45;
  //   const SP = 36;
  //   const KCL = 60;

  //   beratTotal = inputUrea + inputSP + inputKCL;

  // };

  {
    /* <Form className="mx-auto w-50">
        <Form.Group className="my-3">
          <Form.Label className="fw-bold">Urea</Form.Label>
          <Form.Control type="number" placeholder="Jumlah (Kg)" required onChange={(ev) => inputUrea(ev.target.value)} />
        </Form.Group>
        <Form.Group className="my-3">
          <Form.Label className="fw-bold">SP-36</Form.Label>
          <Form.Control type="number" placeholder="Jumlah (Kg)" required onChange={(ev) => inputSP(ev.target.value)} />
        </Form.Group>
        <Form.Group className="my-3">
          <Form.Label className="fw-bold">KCL</Form.Label>
          <Form.Control type="number" placeholder="Jumlah (Kg)" required onChange={(ev) => inputKCL(ev.target.value)} />
        </Form.Group>
      </Form> */
  }
}
